apiVersion: apps/v1
kind: Deployment
metadata:
  name: axion-simulator
spec:
  selector:
    matchLabels:
      app: simulator
  template:
    metadata:
      labels:
        app: simulator
    spec:
      containers:
      - name: axion-simulator-container
        image: brijendra.azurecr.io/simulator-image:6
        ports:
        - containerPort: 8000
